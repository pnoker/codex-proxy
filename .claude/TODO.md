# Role: 高级后端架构师 & AI API 专家

## Background
我目前正在维护和重构一个大模型 API 代理项目。当前项目提供了一个统一的代理接口：`http://localhost:8765/v1`。为了提升项目的扩展性和规范性，我们需要对架构和代码进行升级。

## Phase 1: 架构审查与对齐（请优先输出此部分）
在开始编写或修改代码之前，请你作为架构师，先完成以下分析：
1. **质疑与审查**：审查当前“将所有模型挂载在单一 `/v1` 接口下”的逻辑是否合理？目前的接口设计是否存在缺陷或扩展瓶颈？
2. **标准对齐 (Codex CLI)**：为了确保接下来的重构准确且具备先进性，请列出标准的 `codex cli` 到底需要什么样的接口格式、参数和路由列表？我们的项目需要如何设计才能完美适配它？

*(请先输出 Phase 1 的分析与设计建议，得到我的确认后，再进行 Phase 2 的代码修改。)*

---

## Phase 2: 核心代码重构任务
在确认架构逻辑无误后，请帮我执行以下 3 项代码重构任务：

1. **模型生态调整**：
   - 清理/移除现有代码中对 `gemini` 模型的所有支持。
   - 优化并重构现有的 `zai` 模型支持逻辑。
   - 新增对 `deepseek` 和 `xiaomi` 模型的完整支持。

2. **路由解耦 (Endpoint 隔离)**：
   - 废弃当前统一的 `http://localhost:8765/v1` 路由模式。
   - 按照模型提供商独立拆分路由，需提供如下结构的代理接口：
     - Zai: `http://localhost:8765/zai/v1`
     - DeepSeek: `http://localhost:8765/deepseek/v1`
     - Xiaomi: `http://localhost:8765/xiaomi/v1`

3. **环境变量管理改造**：
   - 针对 `zai`、`deepseek`、`xiaomi` 等模型提供商的 `Base URL` 和 `API Key`，必须全部改为通过环境变量（`.env` 文件）读取。
   - 代码中严禁硬编码敏感信息及外部接口地址。
   - 请顺便为我生成一份标准的 `.env.example` 模板文件以供参考。